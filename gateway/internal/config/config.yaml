---
server:
  port: 8080
  host_port: localhost:8080
  #log_path: logrus.log

#postgres:
#  host: host.docker.internal #localhost
#  port: 5436
#  user: admin
#  pass: admin
#  name: users
#  ssl_mode: disable

nats:
  url: "nats://nats:4222" #url: "localhost:4222"
  name: "EVENTS"
  #subjects: "events" # subjects: "events.>"
  subjects.topic: "events.users_info"
  #ClusterID: microservice
  #ClientID: test-nats-app
...